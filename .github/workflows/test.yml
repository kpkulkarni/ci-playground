name: My First Robot

on: [push]

jobs:
  check-code:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3

      - name: Run PHP Test
        run: php calculator.php
